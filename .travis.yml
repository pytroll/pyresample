language: python
python:
- '2.6'
- '2.7'
- '3.2'
- '3.3'
- '3.4'
before_install:
- sudo apt-get update -qq
- sudo apt-get install libfreetype6-dev
- sudo apt-get install libgeos-c1 libgeos-dev
install:
- pip install -e ".[pykdtree, quicklooks]" --allow-external basemap --allow-unverified basemap
- pip install coveralls
script: coverage run --source=pyresample setup.py test
after_success: coveralls
